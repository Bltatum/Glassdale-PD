//htmlrep of array
export const witnesses = () => {
    return `
    <article class="witnesses" id="${witnesses.id}">
      <p>${witnesses.name}</p>
      <p>${witnesses.statements}</p>
    </article>
    `
}
